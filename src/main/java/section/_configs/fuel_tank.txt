interface              : IFuelTank
class name             : FuelTank
java archive file name : fuel_tank.jar
section                : Wing
configuration          : numberOfFuelTank = 3

primary flight display : amountOfFuel : int          

events                 : FuelTankTakeOut(amount : int)
                         FuelTankRefill()
                         FuelTankRefill(amount : int)